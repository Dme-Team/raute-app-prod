# 🔐 إعداد Service Role Key لإنشاء حسابات السواقين

## الخطوات المطلوبة:

### 1️⃣ الحصول على Service Role Key من Supabase
1. افتح [Supabase Dashboard](https://supabase.com/dashboard)
2. اذهب لمشروعك (Project)
3. من القائمة الجانبية: **Settings** → **API**
4. ابحث عن قسم **"Project API keys"**
5. انسخ المفتاح اللي اسمه **`service_role` secret**
   ⚠️ **تحذير:** ده مفتاح سري جداً - **لا تشاركه** ولا تحطه في كود بيتعرض للمتصفح!

### 2️⃣ إضافة المفتاح لملف `.env.local`
1. افتح ملف `.env.local` في مجلد المشروع
2. أضف السطر التالي (استبدل `YOUR_SERVICE_ROLE_KEY` بالمفتاح اللي نسخته):

```
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
```

### 3️⃣ إعادة تشغيل السيرفر
- أوقف السيرفر الحالي (Ctrl+C)
- شغله تاني:
```bash
npm run dev
```

---

## ✅ التأكد من نجاح الإعداد
1. افتح Dashboard كـ Manager
2. اذهب إلى **Drivers** → **Add Driver**
3. املأ البيانات:
   - الاسم: Test Driver
   - Email: testdriver@example.com
   - Password: test123456
   - (باقي البيانات اختياري)
4. اضغط **Create Driver Account**

### النتيجة المتوقعة:
- رسالة نجاح تحتوي على Email & Password
- السواق دلوقتي يقدر يدخل بالـ Email والـ Password دي!

---

## 🔥 إذا ظهرت مشكلة "Auth creation error"
ده معناه إن الـ Service Role Key:
- مش موجود في `.env.local`
- أو مش صح
- أو السيرفر محتاج Restart

---

## 📋 الملف النهائي `.env.local` لازم يبقى فيه:
```
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

---

✨ **بعد كده، السيستم هيبقى جاهز لإنشاء حسابات السواقين بشكل كامل من الـ Dashboard!**
